import { Component } from "react";
import { Router } from "../components";

export default class App extends Component {
  render() {
    return (
      <div>
        <Router />
      </div>
    );
  }
}
